# クラゲ水槽アプリ 計画書

参考画像 `1786667042546.png` を**そのままアプリの絵にする**。水槽の中身だけを
くり抜いて、その奥でリアルタイムに水とクラゲを動かす。館内・壁のパネル・床・
アクリルの縁、そして少女は参考画像のまま動かない。

**水槽の中だけが動いているアプリ。**

作り方の骨格は `amanesf/sakura`（空と雲のアプリ『終わりを知らない、あの夏の
境界線』）を踏襲する。参照は同リポジトリの `plan.md` / `image-sky-plan.md` /
`scripts/README.md` / `agent-workflow-policy.md` / `app/src/`。

---

## 1. コンセプト

- **固定カメラ**。プレート（参考画像）と 1:1 で重なる必要があるため動かさない。
- **時間帯の弧は持たない**。館内照明は一定。軸は「個体数」「水流」「水の色」
  「倍速」（§6）。
- **無限に眺められること**が唯一の成功条件。だから**水槽は閉じている**——
  寿命で死ぬ個体はなく、沈んで消える個体もない（`scene/swarm.ts`）。個体が
  増減するのは「個体数」を動かしたときだけで、そのときも到着は奥の壁で7秒、
  退場は5秒かけて水に溶ける。**目の前で個体が消えたり湧いたりしてはならない。**
  「同じ絵に戻らないこと」は寿命ではなく流れ（非周期）と Verlet 鎖が担保する。
  種のカウンタは単調増加で、剰余で戻る個体は存在しない。
- 少女はプレートの中に描かれた存在。3D では扱わない（§5）。

---

## 2. 大方針

### 2.1 アニメの絵であって、現実の再現ではない

制作メモ（『3Dでアニメの雲を描くということ』）の結論をそのまま指針にする。
**3D が得意な要素——正確な光、なめらかな形、精密な影——を一つずつ手放す。**

| 3D の素直な答え | 本作で採る答え | 実装 |
|---|---|---|
| 水の色を吸収係数から出す | 参考画像から測った色ランプを引く | `scene/ramps.ts`（`scripts/ramp.js` が生成） |
| 陰影を N·L でつける | 同じくランプ引き。暗部は灰色でなく**青に寄る** | `scene/water.ts` / `jellyfish.ts` |
| 精密な自己影 | わざと大まかに。4段の粗いポスタライズ | `jellyfish.ts` の `banded` |
| なめらかなドーム | 凹みを入れる（8つのローブが縁を内側へ引く） | `bellVertex` の `scallop` |
| きれいなアンチエイリアス | 筆跡を模した異方性 Kuwahara | `effects/anisotropicKuwahara.ts` |

**物理は「動き」と「構造」に使い、「色」には使わない。** 拍動の非対称周期、
触手の Verlet 追従、水流のカールノイズはすべて式で決める。色は測る。

### 2.2 目視で直さない

- 「参考画像の雰囲気を文章で説明して実装させる」依頼は禁止。
- 色・構図の数値化はメインが先に済ませる。「だいたいこのへん」で置かない。
- 測定基盤（§7）を実装より先に作る。**これは守った**——そして測定でしか
  見つからない不具合が実際に3つ出た（§10）。

---

## 3. 3層構成

| 層 | 中身 | 動く? |
|---|---|---|
| 前 | プレート（館内・壁のパネル・床・円柱上端と縁・**少女**） | 動かない |
| 中 | クラゲ（傘・口腕・触手）とマリンスノー | 動く |
| 後 | 水（散乱と光条、上部照明からの減衰） | 操作盤でゆっくり変わる |

---

## 4. プレート — 実装済み（`scripts/plate.js`）

入力は2枚。sakura が「理想だがめったに手に入らない」と書いている構成。

- **キー済みファイル** `1786669093785.png`：水を手作業でフラットに塗り潰した
  もの。少女はこちらにしかいない。マットはここから取る（クロマキー）。
- **元画像** `1786667042546.png`：少女のいない同じ構図。塗り潰しがアクリルの
  ハイライトを覆っているので、そこだけ元画像から測る。

不透明な画素はすべてキー済みファイルから、塗り潰し内部のアクリルだけ元画像から。

- **許容値 60 は少女が決めた。** 髪がキーから 87、シャツが 85 の距離にあり、
  sakura の 105 では彼女が穴だらけになる。
- **キーが少女に乗った箇所**（腕・肩・靴下のアンチエイリアス）はキー色そのもの
  （距離 5〜12）で閾値では分けられない。分けるのは**大きさ**——本物の隙間は
  脚の間 4,070px、うなじとポニーテール周りが 629〜927px、こぼれは 700 個超の
  40px 未満の粒。100px で切ると 160 と 629 の間に何もなく、きれいに分かれる。
- 落とした粒は**塗ってはいけない**（塗る＝キー色を塗る）。周囲から埋める。
- **色は変えない。** 一度は `C_obs = α·C_glass + (1-α)·C_water` を解いて
  ガラスの色を復元していたが、原理的に正しくても絵に対しては誤り——部分被覆の
  ところでアクリルが暗くなった。プレートは「描かれたまま画面に届く」ことが
  すべてなので、計算するのは α だけにした。
- 検証：キー領域はフレームの 62.9%、うち 23.2% がアクリルの部分アルファ。
  **実測した水槽内部の 85.9% が手キーと一致**（手キーと `geom.js` が別の絵を
  見ていないかのチェック）。

---

## 5. 少女とシーン

少女は**手で用意されたプレートの中にいる**。3D 側には何もない。シーンを増やす
場合も同じで、必要なのは「元画像」と「水を塗り潰したコピー」の2枚。
`scripts/plate.js` の `KEYED` と `scripts/reference.js` の `REF` を差し替え、
`scripts/README.md` の再生成手順を順に回す。

生成物はプレート1と**同じフレーム解像度・同じ円柱の位置**でなければならない。
`plate.js` が最後に出す「一致率」がそのチェックになる。

---

## 6. 操作盤（`ui/controls.ts`）

連続量のみ。プリセットボタンは置かない（sakura が積乱雲/快晴の2ボタンを
捨てたのと同じ理由：間の状態も見るべき絵だから）。

| つまみ | 範囲 | 効くもの |
|---|---|---|
| 個体数 | 2〜20体（既定 8） | 群れの頭数。減らすと最古の個体から順に溶けて消える |
| 水流 | 0〜1（既定 0） | カールノイズの振幅、光条の流れ、触手の靡き |
| 水の色 | 0〜1（既定 0.5） | 0.5 は**測った色そのまま**。離れるとインディゴ／青緑へ色相だけ振る（線形光・輝度保存） |
| 倍速 | 1〜8x | `simTime` |

**シーンの状態は固定ステップで進む。** `?t=<秒>` で凍結でき、これが崩れると
計測ハーネス全体が壊れる。Verlet 鎖は閉じた式を持たないので、凍結フレームは
0 から積分して作る（90秒で実時間1秒ほど）。

---

## 7. 測定基盤（`scripts/`）— 実装済み

| スクリプト | 用途 |
|---|---|
| `reference.js` | **参考画像と実測値を名指しする唯一の場所。** 他は全部ここを読む |
| `geom.js` | 円柱と2つの楕円を実測 → カメラを解く → `tank.json` と `app/src/core/measured.ts` を生成 |
| `ramp.js` | 水・傘・口腕の等人口32バケット色ランプ → `scene/ramps.ts` |
| `plate.js` | §4 |
| `capture.js` | Playwright で決定論的キャプチャ |
| `waterprofile.js` | 帯別の色 **と p10-p90 の広がり**（後述） |
| `watermodel.js` | 水シェーダーの CPU 移植。定数を座標降下で解く |
| `hdr.js` | sRGB ↔ トーンマップ前リニア HDR |

実測値（`geom.js`）：フレーム 896x1200、円柱 x0=449.0 R=352.0、上端楕円
yc=110.0 b=59.1、下端楕円 yc=1106.9 b=50.0、焦点距離 3176px、主点 y=651.0、
視点距離 9.02R、水槽高さ 2.83R、視点高さ 1.30R、給排水管 0.205R。

---

## 8. 中身の設計

### 8.1 水（`scene/water.ts`）

円柱との解析交差で得た区間だけを 40 ステップでマーチ。積算するのは**光の量
というスカラー1つ**で、色ではない。色は最後にランプを1回引くだけ。

- 光条は「上からの照明が波打つ水面を通ったもの」として、水面の遮蔽関数から出す。
  個別のライトシャフトは描かない。
- **視線方向への減衰が光条の生死を決める。** 光条は縦の柱で、この構図の視線は
  ほぼ水平なので、重み無しで積分すると柱をいくつも横切って平均化され、どの
  ピクセルも同じ値になる（実測：帯ごとの p10-p90 が 5〜19、参考画像は 17〜70）。
  手前ほど重く積むと構造が残る。物理的にも、濁った水で目に届く光の大半は
  手前で散乱したものなので「見えているのは手前の模様」で正しい。
- 定数は `watermodel.js --solve` が解いた値。ゲインだけは、モデルに含まれない
  ブルーム/Kuwahara の持ち上げ分（実測で緑と青に約 +8）を後段で補正している。

### 8.2 クラゲ（`scene/jellyfish.ts` / `swarm.ts`）

- 傘は極座標のパラメトリックメッシュを頂点シェーダーで変形。収縮 0.25 /
  弛緩 0.75 の非対称拍動、縁は半径の関数で位相が遅れる。
- 傘の放射状の筋（14本）は参考画像から数えた。この大きさでは「クラゲに見える」
  かどうかの大半をこれが決めている。
- 口腕・触手は Verlet 質点ばね鎖。**動きは一切アニメーションしていない**——
  傘にぶら下げて水に任せた結果として S 字も巻き込みも出る。鎖ごとに長さを
  変えてある（同じ長さの鎖は1枚の板のように動き、それがシミュレーション臭さの
  最大の原因になる）。
- 大きさは実測：参考画像の暖色の傘は 90〜105px、水槽の 704px に対して
  半径 0.07R。
- 群れは閉じた個体群。**寿命による退場も、境界による退場も禁止**。個体は中性
  浮力で、床と水面は「壁」ではなく最後の 0.2 半径で効く柔らかい押し返し。
  互いには反発する（O(n²)、n≤20）——これが無いと数分で隅に固まる。
  **剰余で戻す実装は禁止**（`swarm.ts` 冒頭）。
- **透明感は不透明度で決まる。** 傘もロ腕も一度は不透明で描かれていて、それが
  「水で出来たもの」ではなく「水に浮かぶプラスチック」に見えていた原因だった。
  傘は視線が膜に沿うシルエットでだけ濃くなり（正面は水が透ける）、ロ腕はヒダ
  そのものが不透明度を刻む。ロ腕の幅は傘半径の 0.15（以前は 0.34＝塊）。
- **マリンスノーは白い。** 水のランプで着色すると水に溶けて消え、水槽が
  「塗られた面」になる。参考画像では画面内で最も明るい小さなものである。
- 流れは**3つの独立ポテンシャルの本物の curl**。1つのスカラーから3成分を作る
  のは curl ではなく、強い方向性が出て9匹が一直線に並ぶ（実際に並んだ）。

### 8.3 PostFX（`core/postFx.ts`）

`render → bloom → OutputPass → Kuwahara → plate`。プレートは最後、
ディスプレイ空間で合成する（`effects/plateShader.ts`）。半径はすべて
896x1200 のバッファピクセルで、他の解像度では意味を持たない。

---

## 9. 受け入れ基準と現状

| 項目 | 基準 | 現状 |
|---|---|---|
| 構図 | 楕円の主軸/短軸が ±2% | ✅ オーバーレイで確認（`geom.js --overlay`） |
| プレート | 手キーと実測形状の一致 | ✅ 85.9% |
| 水（中央値） | 帯別 RMSE < 5 | ⚠️ 10.9。主因は赤チャンネル（参考 3〜5 に対し 8〜9） |
| 水（構造） | 帯別 p10-p90 が参考と同オーダー | ⚠️ 上部は届いた（52 対 63）、中〜下部が不足 |
| クラゲ | 面積比・輝度勾配・触手長が ±10% | ❌ `jellymeasure.js` 未実装 |
| 再現性 | 同じ `?t=` が同じ絵 | ✅ 固定ステップ＋種のみ（`Math.random` なし） |
| 性能 | 実機 60fps | ❌ 未計測（SwiftShader のみ） |

---

## 10. 測定でしか見つからなかったもの

「調整で直せそう」に見えて、実際には**バグ**だったもの。§2.2 を守った理由。

1. **ノイズの hash が非一様**（平均 0.228、本来 0.5）。コントラストを上げるほど
   水槽が黒くなっていた。CPU に移植して測って初めて分かった。
2. **底の楕円を「最下部の明るい画素」で取ると床の反射を拾う**。楕円が潰れ、
   そこから解いたカメラが 17R 先・画角 11° になっていた。
3. **偽の curl**。9匹が一直線に並んだ。
4. **中央値だけでは足りない**。最初のレンダリングは帯別中央値 RMSE 4.8 で
   基準を通過しながら明らかに間違って見えた——水がどこも同じ色だったから。
   以後 `waterprofile.js` は広がりを併記する。
5. **プレートが暗い**。ポスト後に合成するのに texture を sRGB としてタグ付けて
   いたため、three.js がリニア化して1段半暗くなっていた。

---

## 11. 次にやること

1. 口腕を「襞のある塊」にする（参考画像で最も効いている要素、現状はリボン1枚）。
2. 水の赤チャンネル。ポスト後のグレードで詰める。
3. `jellymeasure.js`（1体単位の統計）。今はクラゲだけ数値の裏付けがない。
4. 実機での性能計測。マーチのステップ数と Verlet の節点数が二大コスト。

## 12. やらないこと

- カメラの移動・インタラクション（固定カメラが測定と最適化の全ての前提）。
- 少女のアニメーション、3D モデリング。
- 参考画像を見ながらの色・形の手動チューニング。
- 音。
