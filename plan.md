# クラゲ水槽アプリ 計画書

参考画像 `1786664132447.png`（暗い水族館の中央に立つ円柱アクリル水槽、青く光る
水の中をオレンジのクラゲが漂い、手前に来館者のシルエット）を**そのままアプリの絵
にする**。水槽の中身だけをくり抜いて、その奥でリアルタイムに水とクラゲを動かす。
館内・副水槽・来館者・アクリルの縁・（後で足す）少女は参考画像のまま動かない。

**水槽の中だけが動いているアプリ。**

作り方の骨格は `amanesf/sakura`（空と雲のアプリ『終わりを知らない、あの夏の境界線』）
を踏襲する。以下、参照は同リポジトリの `plan.md` / `image-sky-plan.md` /
`scripts/README.md` / `agent-workflow-policy.md` / `app/src/`。

---

## 1. コンセプト

- **固定カメラ**。プレート（参考画像）と 1:1 で重なる必要があるため、カメラは
  プレートごとに固定。動くのは水槽の中身だけ。
- **時間帯の弧は持たない**。sakura の正午→夕方に相当する大域変化は無し。館内照明は
  一定。代わりの軸は「クラゲの密度」「水流」「上部照明の色」（§6 の操作盤）。
- **無限に眺められること**が唯一の成功条件。個体は画面外へ抜けたら新しい種で
  作り直し、同じシルエットが二度出ないようにする（sakura のスポーン/デスポーンと
  同じ理由。`image-sky-plan.md` §2）。
- 少女は sakura と同様、**プレートの中に描かれた存在**として置く（§5）。3D で
  モデリングはしない。

---

## 2. 大方針

### 2.1 アニメの絵であって、現実の再現ではない

制作メモ（『3Dでアニメの雲を描くということ』）の結論をそのまま本作の指針にする。
**3D が得意な要素——正確な光、なめらかな形、精密な影——を一つずつ手放す。**
本作での対応：

| 3D の素直な答え | 本作で採る答え |
|---|---|
| 水の色を Jerlov 係数の波長別減衰で出す | **参考画像から測った色ランプを引く。** 水の青は深さではなく「明るさの関数」として測る（sakura `cloudRamp.ts` と同じ方法：輝度でソートして等人口 32 バケットの平均色。明度で引くのではなく人口で引くので、階調のヒストグラムごと参考画像に一致する） |
| クラゲの陰影を N·L でつける | 同じくランプ引き。暗部が灰色に落ちるのではなく**青に寄る**、という参考画像の性質は N·L の乗算からは絶対に出ない（sakura が測って確かめた失敗そのもの） |
| 傘や触手に精密な自己影を落とす | **わざと大まかにする。** 精密にすると「半透明な球の集まり」に見える。塊としてのシルエットを優先 |
| なめらかな傘のドーム | **凹みを入れる。** 凸だけの形は綿菓子になる（`cloudNodule.ts` の教訓がそのまま効く。クラゲの傘の縁の波打ちと口腕の襞は凹凸で出す） |
| きれいなアンチエイリアス | 絵の具の筆跡を模したぼかし（異方性 Kuwahara、`effects/anisotropicKuwahara.ts` をそのまま移植） |

**物理は「動き」と「構造」に使い、「色」には使わない。** 拍動の非対称周期、
触手の Verlet 追従、水流のカールノイズ、屈折の幾何——ここは式で決める。式で決めると
決めた後にパラメータを目分量でいじらない。色は測る。

### 2.2 目視で直さない（sakura `plan.md` §2 / `agent-workflow-policy.md` §1.5）

- 「参考画像の雰囲気を文章で説明して実装させる」依頼は禁止。
- 色の数値化はメインが先に済ませる。サブエージェントに「良い感じの色」を選ばせない。
- 構図・カメラも同じ。「だいたいこのへん」で数値を置かない。
- そのための測定基盤（§7）を**実装より先に**作る。

---

## 3. 3層構成

`image-sky-plan.md` の構成をそのまま置き換える。

| 層 | 中身 | 動く? |
|---|---|---|
| 前 | 参考画像のプレート（館内・副水槽・来館者・少女・床・円柱上端の楕円・アクリルの縁とハイライト） | 動かない |
| 中 | クラゲ（傘・口腕・触手）とマリンスノー | 動く |
| 後 | 水（散乱と光条、上部照明からの減衰） | 操作盤でゆっくり変わる |

前面プレートは参考画像の**水以外の全画素**なので、絵の質はそのまま保たれる。
作るのはアクリルの向こう側だけ。

---

## 4. プレート（水のくり抜き）

`scripts/plate.js` を移植して `app/public/plate.webp`（RGBA、参考画像の原寸）を作る。
多角形を手で描かず**フラッドフィル**で取る（sakura と同じ。境界の描かれたままの
輪郭が出る）。

くり抜く範囲は円柱の内側。キーイングの見立て：

- 水（明るい青、B-R が大きい）と館内（ほぼ黒〜暗い青灰）は輝度でも彩度でも十分
  離れているので、sky より易しい。閾値は実測して決める。
- **境界の性質で扱いを分ける**（ここが sakura と同じく作業の本体）：
  - 円柱の上端の楕円・下端の台座・床との境 → 硬いエッジ。1px フェザー。
  - **アクリルの縦のハイライト**（円柱の左右と中央に走る白い帯）・上端の水面の
    白い滲み・右下のスパークル → **部分アルファ**。ここは「水 × アクリル」の
    合成結果なので、α=0 だとハイライトが消え、α=1 だと水が動かない。
    観測色から水の色を引いて解く（`C_obs = α·C_glass + (1-α)·C_water`）。
    参考画像でアクリルらしさを作っているのは主にこの帯なので、**手を抜くと
    一気に安っぽくなる最重要箇所**。
  - 参考画像のクラゲ → **プレートに残さず全部くり抜く。** その役はエンジンがやる。
  - 中央の柱（給排水管）→ **プレートに残さず、3D の円柱として描く。** 色は
    ほぼ平坦なので再現は容易で、残してしまうとクラゲが柱の後ろを通れない。
  - 手前の来館者のシルエットは**プレートに残す**（動かす必要がない）。
- 検証：プレートを「参考画像の水＋クラゲ」の上に戻して合成し、参考画像との
  RMSE < 2 / 最大差 < 12。

---

## 5. 少女とシーン

sakura は 1 枚の絵から始めて、`scene/scenes.ts` で複数プレート（教室の窓辺／軒下／
海沿いのバス停）に広げた。本作も同じ道を採る。

- **プレート 1（現行の参考画像）**：来館者のシルエットのみ。これが全ての統計を
  fit する基準フレームであり、既定シーン。**後からシーンを足しても、この 1 枚の
  測定値は動かさない**（sakura `scenes.ts` の設計思想）。
- **プレート 2 以降**：少女が円柱の前に立つ構図、通路側からの構図など。
  `scripts/gemini_call.js` を移植し、参考画像を渡した image-to-image で生成する
  （文章だけで描かせない）。生成物はプレート 1 と**同じフレーム解像度・同じ円柱の
  位置**でなければならない。ここが揃わないと 3D 側と重ならないので、生成の
  受け入れ基準に円柱の楕円の位置ずれ ±1% を入れる。
- シーンごとに変わるのは、プレート／円柱の画面内位置と半径／カメラのピッチ。
  sakura が `SceneDef` に持っている `horizonRow` 相当は、本作では
  **円柱の上端・下端の楕円のパラメータ**になる。
- 少女のアニメーション（瞬き・髪の揺れ）はやらない（sakura §8 と同じ）。

---

## 6. 操作盤（`ui/controls.ts` を移植）

sakura は「訪れた人に、彼女が触れているものと同じ操作盤を手渡す」構成。本作も
同じにする。軸は連続量にし、プリセットボタンは置かない（sakura が
積乱雲/快晴の 2 ボタンを捨てたのと同じ理由：間の状態も見るべき絵だから）。

| つまみ | 範囲 | 効くもの |
|---|---|---|
| 個体数 | 疎（2体）〜 群れ（20体） | スポーン器の密度 |
| 水流 | 静止 〜 強い | カールノイズの振幅と流速、触手の靡き |
| 上部照明 | 青白 〜 藍 | 水中散乱の光源色（色は測ったランプ上の移動として定義する） |
| 倍速 | 1〜30倍 | `simTime` |
| フレームレート | 30 / 60 | 実機用 |

**シーンの状態は `simTime` の純関数**にする（毎フレーム加算しない）。`?t=<秒>` で
直接指定でき、これが崩れると計測ハーネス全体が壊れる（sakura `image-sky-plan.md` §4）。

---

## 7. 測定基盤（`scripts/`）

**フェーズ 1 でこれを作る。**sakura で最も価値があった資産。

| スクリプト | 由来 | 用途 |
|---|---|---|
| `capture.js` | 移植 | Playwright で決定論的キャプチャ。`?t=` で凍結、`?fit=frame` で画面全部を絵にする |
| `crop.js` | 移植 | 参考画像とレンダリングから同じ box を切る |
| `hdr.js` | 移植 | sRGB ↔ トーンマップ前リニア HDR。**参考画像から取った色をそのまま定数に書かないため**（sakura はここで複数の定数を間違えた） |
| `plate.js` | 移植＋改修 | §4 のキーイング |
| `gemini_call.js` | 移植 | プレート 2 以降の生成 |
| `waterprofile.js` | 新規（`skyprofile.js` 相当） | 円柱内部の高さ方向バンドごとの色・輝度プロファイル |
| `jellyramp.js` | 新規（`cloudRamp.ts` の生成側） | 参考画像のクラゲ／水から等人口 32 バケットの色ランプを抽出 |
| `jellymeasure.js` | 新規（`measure.js` 相当） | クラゲ 1 体の統計：傘の面積・アスペクト、輪郭の凹凸（`bumpR`/`bumpDepth` 相当）、内部の輝度勾配、触手の長さ・曲率 |
| `tonedist.js` | 移植 | 階調がどこに乗っているか（`measure.js` は広がりしか見ない） |

注意（sakura の教訓をそのまま）：

- 統計は必ず参考画像とレンダリングの**両方に同じ手順で**かける。
- クラゲは 1 体ずつクロップしてから測る。全景のまま測ると他個体と混ざって
  どの統計も薄まる。
- キャプチャ中に `app/src` を編集しない（dev server の HMR が結果に混入する）。
- ソルバーは 0.1 ポイントの RMSE のために物理的にありえない値に張り付く。
  範囲を先に決め、張り付いたら人間が戻す。

---

## 8. 中身の設計

### 8.1 水

- 円柱との解析交差で `[tNear, tFar]` を取り、その区間だけ 32〜48 ステップで
  マーチ。画面のうち円柱の内側しか計算しない。
- 各ステップで（a）減衰、（b）上部照明からの散乱、（c）クラゲによる遮蔽を積む。
- **色はランプ引き**（§2.1）。積算した光学的な量 `s∈[0,1]` を、`waterprofile.js`
  で測った水のランプのインデックスにする。
- 光条（参考画像で上から差している縦の帯）は、水面の波のノイズを通した照明の
  遮蔽から出す。個別に描かない。
- マリンスノーは一様分布の点群（撹拌された水槽なので深さで密度を変えない）。

### 8.2 クラゲ

参考画像に大きく 2 種いる。

- **オレンジの傘（主役、4〜5 体）**：ドーム型。傘が内側から橙に光って見えるのは
  上からの照明が薄い傘を透過しているため。厚み依存の透過として構造は物理で、
  色はランプで出す。
- **白く細長いリボン状（背景）**：口腕・触手が主体で傘は小さい。実質
  「Verlet 鎖の描画」。

実装：

- 傘は極座標のパラメトリックメッシュ（半径 16 × 周 32）を頂点シェーダーで変形。
  拍動は収縮 0.25 / 弛緩 0.75 の非対称イージング、縁は半径の関数で位相を遅らせる
  （ひらひらして見える主因）。**凹みを入れる**（§2.1）。
- 触手・口腕は Verlet 質点ばね鎖を CPU で（1 体あたり 8 本 × 24 節点、総計 3000
  節点以下なら 60fps を割らない見込み。足りなければ**測ってから** GPU に移す）。
- 自己影は落とさない／落としても大まかに。精密な影は塊感を壊す。
- 前後関係が要るので、水のマーチと同じパスで深度順に挿し込む。
- 個体数は参考画像の被覆率に合わせて基準点を決め、操作盤の「個体数」は
  そこからのスケールとして定義する（sakura の雲量と同じやり方）。
- **寿命とスポーン**：画面外へ抜けた個体は新しい種で作り直す。剰余で戻さない。

### 8.3 アクリルの屈折

参考画像で最も効いている要素で、左右の副水槽が細く潰れて見えるのはこれ。ただし
**副水槽はプレート側にいる**ので、その歪みは既に絵に描かれている。3D 側で解くのは
「水槽の中のもの（クラゲ・光条）が縁に近づいたときの歪み」だけ。

- 円柱面の法線から Snell（水 1.333 / アクリル 1.49 / 空気 1.0）で屈折方向を出し、
  画面空間で UV をずらす。フレネル反射は Schlick。
- 係数は屈折率から決まるので調整の余地がない（＝それでよい）。

### 8.4 PostFX

順序は「水とクラゲを描く → **窓（円柱）の中だけに** PostFX → プレートを合成」。
プレートに掛けると参考画像の絵が壊れる（sakura §6 と同じ）。

- ブルーム（クラゲの発光と光条）／異方性 Kuwahara（筆跡）／マクロコントラスト／
  決定論的トーンカーブ。すべて sakura から移植し、**定数は本作の参考画像に
  対して測り直す**（バッファピクセル単位で fit されているため、そのままの数値は
  意味を持たない）。

---

## 9. 実装フェーズ

1. **土台**：`app/` を vite + three.js + TypeScript で構築。sakura の `app/src/core/`
   （renderer / camera / frame / postFx / compose）の構造を踏襲。特に
   **レンダーバッファを参考画像の解像度に固定し canvas へスケールして出す**設計
   （`main.ts`）は必ず継承する。`base: '/jellyfish/'` と Pages の workflow も移植。
2. **測定基盤**（§7）。1 と並列可。
3. **プレート**（§4）。単色の水を裏に置いた時点で、アプリの見た目はほぼ完成する。
   ここまでで一度デプロイして絵を確認する。
4. **合成パイプライン**：水 → PostFX → プレート、レターボックス（`core/frame.ts`）。
5. **構図の確定**：円柱の半径・高さ・カメラ FOV・視点高さを、上端／下端の楕円から
   逆算。プレートと 3D の円柱がピクセルで重なることを検証。
6. **水**（§8.1）。`waterprofile.js` で合わせ込む。クラゲはまだ無し。
7. **クラゲ・傘**（§8.2）。`jellyramp.js` のランプ、`jellymeasure.js` で 1 体単位の統計。
8. **クラゲ・触手**（Verlet）。7 に依存。
9. **群れとスポーン**（水流・寿命・個体差）。8 に依存。
10. **アクリルの屈折**（§8.3）。6 以降なら 7〜9 と並列可。
11. **PostFX とグレーディング**（§8.4）。
12. **操作盤**（§6）。
13. **シーン追加と少女**（§5）。
14. **パフォーマンス調整**。マーチのステップ数と Verlet の節点数が二大コスト。
    **測ってから**削る。

### 受け入れ基準（数値で持つ）

- 構図：円柱上端・下端の楕円の主軸長・短軸長が参考画像と ±2%。
- プレート：§4 の合成 RMSE < 2。
- 水：`waterprofile.js` の 13 バンドプロファイルの RMSE < 5（sRGB 0-255）。
- クラゲ：傘の面積比・輝度勾配・輪郭の凹凸・触手長が参考の該当個体と ±10%。
- 階調：`tonedist.js` の分布が参考画像とバイモーダルにならず連続（sakura で
  実際に壊れていた指標）。
- 再現性：固定フレームがバイト単位で再現する（compose を通さない測定パスを残す
  ——sakura `postFx.setRenderToScreen` と同じ理由）。
- 実機で 60fps、最低 45fps を下回らない。

---

## 10. 進め方（モデルとサブエージェント）

`agent-workflow-policy.md` をそのまま適用。

- **シェーダー / GLSL、パーティクル・物理挙動の設計、パフォーマンス調整、見た目の
  質にかかわる反復判断は自動的に Opus。**本作はフェーズ 6〜11 がほぼ全部これ。
- 配線・UI・ドキュメント通りの実装は Haiku。同一タスクで 2 回失敗したら即
  エスカレーション（3 回目を投げない）。
- 依頼はサブシステム単位（＝§9 のフェーズ 1 つ）。関数単位で刻まない。
- 共有インターフェース（クラゲ 1 体の状態の型、水のボリュームへのクエリの
  シグネチャ）は、実装を投げる前にメインが先に決めて渡す。

---

## 11. やらないこと

- カメラの移動・インタラクション（固定カメラが測定と最適化の全ての前提）。
- 少女・来館者のアニメーション、3D モデリング。
- クラゲの種の網羅（参考画像の 2 種のみ）。
- 参考画像を見ながらの色・形の手動チューニング。
- 音。

---

## 12. 最初にやる 3 つ

1. `app/`（vite + three + TS の土台）と `scripts/`（`capture.js` / `crop.js` /
   `hdr.js` / `plate.js` の移植）。
2. プレートのキーイング。特にアクリルの縦ハイライトの部分アルファ（§4）。
3. 参考画像から円柱の楕円を測り、構図の数値（半径・高さ・FOV・視点高さ）を確定して
   本ドキュメントに追記する。

この 3 つが終わるまでシェーダーの色定数を 1 つも書かない。
