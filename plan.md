# クラゲ水槽シミュレーター 計画書

添付参考画像（暗い水族館の中央に立つ円柱アクリル水槽、青く光る水中を
オレンジ色に発光するミズクラゲ／アカクラゲが漂い、手前に来館者のシルエット）
を目標とする、ブラウザで動く高品質な常設ビジュアル。

設計・運用の骨格は `amanesf/sakura`（夏雲シミュレーター）を踏襲する。
以下、参照は同リポジトリの `plan.md` / `scripts/README.md` /
`agent-workflow-policy.md` / `app/src/`。

---

## 0. 前提：参考画像をリポジトリに置く

sakura の全工程は「参考画像のピクセルに対して統計を取る」ことで成立している
（`scripts/README.md` の measure loop）。したがって**最初にやることは実装では
なく、参考画像をリポジトリ直下にコミットすること**。

- 添付画像を `reference.png`（896x1195 程度、元解像度のまま無加工）として置く。
- 以後、この plan と全スクリプトはこのファイル名だけを参照する。sakura では
  参考画像を差し替えた際、古い画像の統計に合わせたシェーダー定数が残って混乱
  した経緯がある（`scripts/README.md` 冒頭）。**参考画像は増やさない・消さない・
  差し替える時は全定数を引き直す。**

---

## 1. コンセプト

- **固定カメラ**。参考画像とほぼ同一の構図（やや見上げ気味、円柱水槽が画面の
  縦いっぱい、下端 1/6 に床と来館者のシルエット）から動かさない。カメラを
  固定することは sakura と同じく最大の最適化予算であり、かつ「参考画像と同じ
  絵になっているか」を毎回同じ切り出し box で測れるという測定上の前提でもある。
- **時間軸は"漂い"のみ**。sakura の時間帯タイムラプスに相当する大域パラメータは
  持たない。館内照明は常に一定。動くのはクラゲの拍動・漂流、水中の光条の揺らぎ、
  微粒子（マリンスノー）、来館者のわずかな動きだけ。
- **無限に眺められること**が唯一の成功条件。ループの継ぎ目が見えない、同じ
  シルエットが繰り返し現れない、という点を明示的な受け入れ基準にする。
- スコープは**水槽・水・クラゲ・館内の光**まで。来館者は板ポリのシルエットで
  済ませる（§7 スコープ外）。

---

## 2. 大方針：sakura から引き継ぐもの／変えるもの

### 2.1 引き継ぐ：数学的決定論と measure loop

sakura `plan.md` §2 の「色や形を目視で調整しない、物理法則と数式から導出する」
方針をそのまま採用する。本プロジェクトでの対応表：

| 見た目の要素 | 決定論的な根拠 |
|---|---|
| 水の色と深さ方向の減衰 | Jerlov 海水型の波長別吸収・散乱係数による Beer-Lambert 減衰（赤が先に落ちて青緑が残る、を式から出す） |
| 水中の光条（ゴッドレイ） | 単一散乱の積分（Henyey-Greenstein 位相関数、g≈0.85 の前方散乱優勢）。`pow(dot,N)` の手癖ハックは書かない |
| アクリル円筒越しの歪み | 円柱面の法線から Snell の法則で屈折（水 n=1.333 / アクリル n=1.49 / 空気 n=1.0 の3界面）＋ Schlick 近似のフレネル反射 |
| クラゲの傘の拍動 | ジェット推進モデル：収縮（速い）→弛緩（遅い）の非対称周期。実測の拍動周期 0.8–1.4Hz、推進は排水量に比例 |
| 触手・口腕の運動 | Verlet 積分の一次元質点ばね鎖＋水の抗力（受動的に追従させる。手付けアニメーションは作らない） |
| 個体の遊泳経路 | カールノイズ（発散ゼロ場）による水流＋鉛直方向の走光性バイアス |
| 発光（傘のオレンジ） | 半透明体のサブサーフェス近似。傘の厚みに対する透過を Beer-Lambert で、逆光時の輪郭光を厚み依存で導出 |
| グレーディング | 露出と館内照度から連続的に導く固定トーンカーブ（キーフレーム hex 指定はしない） |

そして sakura の `scripts/` に相当する測定基盤を最初に作る（§5 フェーズ1）。
これがない状態でシェーダー定数を書き始めることを禁止する。

### 2.2 引き継ぐ：参考画像からの数値化はメインがやる

`agent-workflow-policy.md` §1.5 の絶対ルール。「参考画像の雰囲気を文章で説明して
実装させる」依頼形式は本プロジェクトでも禁止。色が必要なら、依頼を出す前に
参考画像から直接ピクセルサンプリングして固定値で渡す。

### 2.3 変えるもの：画像切り抜きではなく完全な手続き生成

sakura は途中で「空は参考画像を切り抜いて動かす」方針に転換した
（`image-sky-plan.md`）。**本プロジェクトはこれを採らない。**理由は、本作の
主題であるクラゲが画面内を大きく動き回り、互いに前後し、光条とアクリルの
歪みを受けるため、静止画の切り抜きでは成立しないから。参考画像は
**測定のターゲットとしてのみ**使い、描画は全て手続き生成する。

これは sakura が最初に失敗した道でもあるので、失敗しないための担保を先に置く：
**フェーズ1（測定基盤）を完成させてからでないとフェーズ3以降に進まない。**

### 2.4 変えるもの：レイマーチ予算の置き場所

sakura は画面全体がボリューメトリック雲だった。本作でボリューメトリックが要る
のは「水中の散乱」と「クラゲの半透明な傘」の2つだけで、どちらも画面の一部しか
占めない。円柱水槽の内側だけを解析的な交差判定でマスクし、その中でのみ
マーチする。背景（館内・他の水槽・床）は通常のラスタライズで十分。

---

## 3. 技術方針の詳細

### 3.1 レイヤー構成（奥→手前）

1. **館内背景**：暗い金属質の壁面、左右に並ぶ副水槽（青緑に光る矩形。中身は
   ぼかした低解像度テクスチャで足りる）、天井配管。ほぼ黒に近い低輝度で、
   主水槽の明るさを引き立てる役割しか持たない。
2. **来館者シルエット**：ビルボード板ポリ。完全な黒ではなく、水槽からの
   青い照り返しを縁に受ける（リムライトのみ物理的に計算）。
3. **円柱水槽（主役）**：外面フレネル反射 → アクリル厚みの屈折 → 内部の水の
   ボリューム（散乱・光条・マリンスノー）→ クラゲ群 → 中心の柱（給排水管）。
4. **水面**：円柱上端。下から見上げる構図なので**全反射（臨界角 48.6°）が
   支配的**で、参考画像上端の白く滲んだ帯はこれ。ここを式で出せると一気に
   それらしくなる。
5. **PostFX**：ブルーム（クラゲの発光と光条にのみ効かせるため、輝度閾値では
   なく専用の emissive バッファを持つ）、色収差わずか、ビネット、
   決定論的トーンカーブ。

### 3.2 クラゲ

参考画像には**大きく2種**いる。

- **オレンジの傘＋短い口腕**（画面中央の主役、4〜5体）：ミズクラゲ寄りの
  ドーム型。傘が内側から橙色に発光して見えるのは、上方からの照明が薄い傘を
  透過しているため。この「透過の色」は水の吸収を通っていない直接光なので暖色
  のまま残る、と説明がつく。ここは式から出す。
- **白く細長いリボン状**（背景、糸を引くように長く伸びる）：アカクラゲの
  口腕・触手が主体で、傘は小さい。これは実質「Verlet 鎖のレンダリング」で、
  §2.1 の質点ばねをそのまま可視化したもの。

実装方針：

- 傘は**極座標のパラメトリックメッシュ**（半径方向 16 分割、周方向 32 分割）を
  頂点シェーダーで変形。拍動は収縮 0.25 / 弛緩 0.75 の非対称イージングで、
  縁が遅れて追従する位相差を半径の関数として入れる（実際のクラゲの縁が
  ひらひらして見える主因）。
- 触手は Verlet 鎖を **CPU で** 回す（1体あたり 8 本 × 24 節点程度、
  総計 3000 節点以下なら 60fps を割らない）。GPU に持っていくのは、実機で
  足りないと**測って**わかってから。
- 描画は前後関係が重要なので、水のボリュームと同じパスで解く。クラゲを
  深度で並べ替えた順に、水の散乱積分の中に挿し込む。
- 個体数は 8〜10。多いほど良いわけではなく、参考画像の密度に合わせる
  （画面内の被覆率を測って合わせる。§5 の受け入れ基準）。

### 3.3 水と光条

- 水槽内部は円柱との解析交差で `[tNear, tFar]` を得て、その区間だけ 32〜48
  ステップでマーチ。ステップごとに（a）水の吸収、（b）上方の照明からの
  単一散乱、（c）クラゲによる遮蔽を積む。
- 光条は「上から差す照明が水面で屈折して入る」ものなので、水面の波を
  ノイズで作れば**コースティクスと光条が同じ計算から出る**。個別に
  ライトシャフトを描かない。
- マリンスノー（参考画像に無数にある白い点）は、水の中に一様分布させた
  点群を距離減衰＋前方散乱の輝度で描く。深さで密度は変えない（撹拌された
  水槽なので一様が物理的に正しい）。

### 3.4 アクリルの歪み

参考画像で最も効いている要素。円柱を通して見るため、**水槽の縁に近いほど
背景が横方向に強く圧縮される**（左右端の副水槽が細く潰れて見えるのはこれ）。

- 画面空間の屈折で解く：まず背景（レイヤー1〜2）をオフスクリーンに描き、
  円柱面の法線から求めた屈折ベクトルで UV をずらして再サンプリング。
- 縁ほどフレネル反射率が上がるので、同じ法線から反射成分も出す。ここも
  Schlick で、係数は屈折率から決まるので調整の余地がない（＝それで良い）。

### 3.5 グレーディング

暗所の水族館なので、ハイライトはクラゲと光条だけ、中間調は狭く、
黒は締める。露出は「画面の 99.5 パーセンタイル輝度を 1.0 に置く」という
規則で自動決定し、手でいじらない。

---

## 4. 測定基盤（sakura の `scripts/` に相当）

**フェーズ1でこれを作る。**sakura で最も価値があった資産であり、
「レンダリング→目視→修正」ループを構造的に潰す唯一の手段。

移植・新規作成するもの：

| スクリプト | 由来 | 用途 |
|---|---|---|
| `capture.js` | sakura ほぼそのまま | Playwright で決定論的キャプチャ。`?t=<秒>` でシーンを固定、`?fit=frame` で画面全部を絵にする |
| `crop.js` | そのまま | 参考画像・レンダリング双方から同じ領域を切り出す |
| `hdr.js` | そのまま | sRGB ↔ トーンマップ前リニア HDR の変換。**参考画像から取った目標色をそのままシェーダー定数に書かない**ための必須ツール（sakura ではここで複数の定数を間違えた） |
| `watercolumn.js` | 新規（`skyprofile.js` 相当） | 水槽内部の高さ方向バンドごとの色・輝度プロファイル。参考画像とレンダリングを同じバンド数で比較 |
| `jellymeasure.js` | 新規（`measure.js` 相当） | クラゲ1体を切り出しての統計：傘の面積・アスペクト、輪郭のスカラップ、内部の輝度勾配（発光が傘の中でどう分布しているか）、触手の平均長／曲率 |
| `bloomprofile.js` | 新規 | 発光点からの輝度の距離減衰カーブ。ブルームの半径と強度を目分量で決めないため |
| `watermodel.js` | 新規（`skymodel.js` 相当） | 水中散乱シェーダーの CPU 移植。キャプチャが遅いので、係数は実レンダリングではなくこちらの座標降下で解く |

sakura の教訓をそのまま持ち込む注意書き：

- 統計は**必ず参考画像とレンダリングの両方に同じ手順で**かける。片側だけの
  数字は根拠にならない。
- ソルバーは 0.1 ポイントの RMSE のために物理的にありえない値に張り付く。
  物理的に妥当な範囲を先に決めて境界を置き、張り付いたら人間が戻す。
- キャプチャ中に `app/src` を編集しない（dev server の HMR が結果に混入する）。

---

## 5. 実装フェーズ

依存順。並列にできるところは明記する。

1. **土台**：`app/` を vite + three.js + TypeScript で新規構築。sakura の
   `app/src/core/`（renderer / camera / frame / postFx / compose）の**構造を
   踏襲**する。特に「レンダーバッファを参考画像の解像度に固定し、canvas へは
   スケールして出す」という `main.ts` の設計は必ず継承する。全ての定数が
   バッファピクセル単位で fit される以上、これを外すと画面ごとに違う絵になる。
   GitHub Pages 用の `base: '/jellyfish/'` と deploy workflow も sakura から移植。
2. **測定基盤**（§4）。1 と並列可。参考画像さえあれば app は要らない。
3. **構図の確定**：円柱の半径・高さ・カメラ FOV・視点高さを、参考画像の
   円柱上端／下端の楕円の見え方から**逆算**して決める（「だいたいこのへん」で
   置かない — `agent-workflow-policy.md` §1.5）。この段階の成果物は
   グレー1色のシリンダーと床だけのシルエット一致確認。
4. **水のボリューム**：吸収・単一散乱・光条・マリンスノー。`watercolumn.js` で
   参考画像のプロファイルに合わせ込む。この時点でクラゲは無し。
5. **アクリルの屈折・反射・水面の全反射**。4 に依存。
6. **クラゲ（傘）**：メッシュ、拍動、サブサーフェス透過。`jellymeasure.js` で
   1体単位の統計を合わせる。
7. **クラゲ（触手・口腕）**：Verlet 鎖。6 に依存。
8. **群れの制御**：カールノイズ水流、個体差（大きさ・拍動周期・色相のわずかな
   ばらつき）、画面外との出入り。7 に依存。
9. **背景（館内・副水槽・来館者シルエット）**。3 以降なら 4〜8 と並列可。
10. **PostFX とグレーディング**：ブルーム、トーンカーブ。`bloomprofile.js` で
    合わせる。
11. **パフォーマンス調整**：モバイル実機。マーチのステップ数と Verlet の
    節点数が二大コスト。**測ってから**削る。

### 受け入れ基準（フェーズごとに数値で持つ）

- 構図：円柱上端・下端の楕円の主軸長・短軸長が参考画像と ±2% 以内。
- 水：`watercolumn.js` の 13 バンドプロファイルの RMSE が 5 以下（sRGB 0-255）。
- クラゲ：傘の面積比・輝度勾配・触手長が参考画像の該当個体と ±10% 以内。
- 全体：正午相当の固定フレームがバイト単位で再現する（sakura の
  `postFx.setRenderToScreen` と同じ理由で、compose を通さない測定パスを残す）。
- パフォーマンス：目標実機で 60fps、最低 45fps を下回らない。

---

## 6. 進め方（モデルとサブエージェント）

`agent-workflow-policy.md` をそのまま適用する。要点だけ再掲：

- **シェーダー / GLSL、パーティクル・物理挙動の設計、パフォーマンス調整、
  見た目の質にかかわる反復判断は自動的に Opus。**本プロジェクトはフェーズ
  4〜8 と 10 がほぼ全部これに該当する。
- 配線・UI・ドキュメント通りの実装は Haiku。同一タスクで 2 回失敗したら
  即エスカレーション、3 回目を投げない。
- 依頼は**サブシステム単位**（＝§5 のフェーズ 1 つ）。関数単位で刻まない。
- 複数フェーズが参照する共有インターフェース（クラゲ1体の状態を表す型、
  水のボリュームへのクエリ関数のシグネチャ）は、実装を投げる前にメインが
  先に決めて渡す。

---

## 7. スコープ外（今回やらないこと）

- 来館者の本格的なモデリング・アニメーション（シルエット板ポリで固定）。
- カメラの移動・インタラクション（固定カメラが全ての最適化と測定の前提）。
- クラゲの種の網羅（参考画像に写っている 2 種のみ）。
- 参考画像を見ながらの色・形の手動チューニング（§2.1 の方針）。
- 音。

---

## 8. 最初にやる3つ

1. `reference.png` をコミットする。
2. `app/`（vite + three + TS の空の土台）と `scripts/`（`capture.js` /
   `crop.js` / `hdr.js` の移植）を作る。
3. 参考画像から円柱の楕円を測り、構図の数値（半径・高さ・FOV・視点高さ）を
   確定して本ドキュメントに追記する。

この 3 つが終わるまでシェーダーの色定数を 1 つも書かない。
